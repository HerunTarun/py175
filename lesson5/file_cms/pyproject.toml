[project]
name = "file-cms"
version = "0.1.0"
description = ""
authors = [
    {name = "Herun Tarun",email = "herun.s@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "gunicorn",
    "uuid",
    "markdown (>=3.10.3,<4.0.0)",
]

[tool.poetry]
packages = [{include = "file_cms", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
